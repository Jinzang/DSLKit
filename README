=head1 NAME

DSLKit -- A set of classes for creating a Domain Specific Language

=head1 USAGE

You create a new language by adding classes to the base code. Each class
implements a new command and are named <Name>Command, where <name> is the
command. Commands use DSLVar, DSLBlock, DSLCode, or DSLCmd as their base class.

More documentation will follow.
